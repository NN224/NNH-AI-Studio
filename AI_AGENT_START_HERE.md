# 🤖 AI Agent Strict Rules

> ⚠️ **اقرأ هذا الملف كاملاً قبل أي عمل - لا استثناءات**

---

## 🚨 القواعد الذهبية (MUST FOLLOW)

### 1. قبل إنشاء أي ملف جديد:

```
□ هل الملف موجود مسبقاً؟ → ابحث أولاً
□ هل يمكن إضافة الكود لملف موجود؟ → أضفه هناك
□ هل الملف الجديد سيُستخدم من أكثر من مكان؟ → إذا لا، لا تنشئه
□ هل أضفت import/export للملف الجديد؟ → تأكد من الربط
□ هل حدّثت index.ts إذا كان موجوداً؟ → حدّثه
```

### 2. قبل حذف أي كود:

```
□ هل بحثت عن استخداماته؟ → grep -r "functionName" .
□ هل هو مستخدم في ملف آخر؟ → لا تحذف
□ هل هو exported؟ → قد يكون مستخدماً خارجياً
```

### 3. قبل تعديل أي ملف:

```
□ هل قرأت الملف كاملاً؟ → اقرأه
□ هل فهمت السياق؟ → إذا لا، اقرأ الملفات المرتبطة
□ هل التعديل يكسر شيئاً آخر؟ → تحقق من imports
```

### 4. بعد أي تعديل:

```
□ npm run lint → يجب أن يمر بدون أخطاء جديدة
□ npm run build → يجب أن ينجح
□ هل أضفت console.log؟ → احذفه قبل الانتهاء
```

---

## 🔴 ممنوع منعاً باتاً

| ❌ لا تفعل             | ✅ افعل بدلاً           |
| ---------------------- | ----------------------- |
| إنشاء ملف `.md` جديد   | استخدم الملفات الموجودة |
| إنشاء ملف `utils` جديد | أضف للـ `lib/` الموجود  |
| إنشاء hook جديد        | تحقق من `hooks/` أولاً  |
| حذف كود "غير مستخدم"   | ابحث عنه أولاً          |
| استخدام `any`          | استخدم type محدد        |
| تجاهل lint errors      | أصلحها فوراً            |
| إنشاء ملف test منفصل   | أضف للـ test الموجود    |
| hardcode values        | استخدم constants/env    |

---

## 📁 هيكل الملفات (لا تغيره)

```
app/                    # Pages & API routes فقط
components/             # React components فقط
hooks/                  # Custom hooks فقط
lib/                    # Utilities & helpers
server/actions/         # Server actions فقط
types/                  # TypeScript types فقط
```

**قاعدة:** كل ملف جديد يجب أن يكون في المجلد الصحيح.

---

## 🔧 عند إصلاح مشكلة

### الخطوة 1: تحقق من وجود prompt

```bash
ls production-fix-prompts/critical/
ls production-fix-prompts/high-priority/
ls production-fix-prompts/medium-priority/
```

### الخطوة 2: إذا وجدت prompt

1. اقرأه **كاملاً**
2. اتبع الخطوات **بالترتيب**
3. تحقق من **كل** معيار قبول
4. شغّل الأوامر المذكورة
5. حدّث الحالة إلى ✅

### الخطوة 3: إذا لم تجد prompt

1. اقرأ `CLAUDE.md`
2. اتبع `.cursorrules`
3. لا تخترع حلولاً معقدة

---

## ⚡ أوامر إلزامية

```bash
# قبل أي PR
npm run lint            # يجب 0 errors جديدة
npm run build           # يجب أن ينجح
npm run test            # يجب أن يمر

# للبحث عن استخدامات
grep -r "functionName" --include="*.ts" --include="*.tsx" .
```

---

## 🎯 Checklist قبل الانتهاء

```
□ لا ملفات جديدة غير ضرورية
□ لا console.log متروك
□ لا any types جديدة
□ لا imports غير مستخدمة
□ npm run lint يمر
□ npm run build ينجح
□ الكود يعمل فعلاً (اختبرته)
```

---

## 📚 الملفات المرجعية

| الملف                               | متى تقرأه            |
| ----------------------------------- | -------------------- |
| `CLAUDE.md`                         | للفهم الشامل للمشروع |
| `.cursorrules`                      | لمعايير الكود        |
| `production-fix-prompts/README.md`  | لقائمة المشاكل       |
| `DATABASE_TYPESCRIPT_INTERFACES.md` | للـ DB types         |

---

## ⚠️ أخطاء شائعة تجنبها

1. **إنشاء ملف ثم نسيان استخدامه** → لا تنشئ ملف إلا إذا ستستخدمه فوراً
2. **نسخ كود بدل إعادة استخدامه** → ابحث عن الموجود أولاً
3. **إصلاح عَرَض بدل السبب** → اذهب للـ root cause
4. **تعديل ملفات `components/ui/`** → هذه shadcn، لا تعدلها
5. **حذف BETA banner** → ممنوع نهائياً

---

## 🔄 Workflow

```
طلب جديد
    ↓
هل هو إصلاح؟ ──نعم──→ تحقق من production-fix-prompts/
    ↓ لا                         ↓
اقرأ CLAUDE.md            وجدت prompt؟
    ↓                      ↓ نعم    ↓ لا
نفذ المهمة              اتبعه    ← ─ ─ ┘
    ↓                      ↓
npm run lint ←─────────────┘
    ↓
npm run build
    ↓
تأكد من الـ checklist
    ↓
انتهى ✅
```

---

**🎯 القاعدة الأهم: لا تترك فوضى. كل ملف له غرض، كل سطر له سبب.**

---

## 📋 للمستخدم: قوالب طلبات جاهزة

**انظر `AI_PROMPTS_TEMPLATES.md`** لقوالب طلبات جاهزة يمكنك نسخها ولصقها.
