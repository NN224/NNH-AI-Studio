# 🚀 NNH AI Studio - BETA Status Guide

## 📋 معلومات المشروع

- **الحالة الحالية:** BETA (تطوير نشط)
- **النسخة:** 0.9.0-beta

### 🌍 البيئات:

#### 🖥️ Development (التطوير - محلي فقط)
- **الرابط:** `http://localhost:5050`
- **الاستخدام:** للتطوير والاختبار الشخصي فقط
- **الوصول:** محلي على جهاز المطور فقط
- **التشغيل:** `npm run dev`

#### 🌐 Production (الإنتاج - منشور للعالم)
- **الرابط:** `https://www.nnh.ae`
- **الاستخدام:** النسخة الحية المنشورة للمستخدمين حول العالم
- **الوصول:** عام - أي شخص يمكنه الوصول عبر الإنترنت
- **التشغيل:** `npm run build && npm run start` (على خادم الإنتاج)

> **⚠️ مهم جداً:** المشروع على `nnh.ae` هو للعالم كله، وليس للتطوير!

---

## ⚠️ قواعد مهمة جداً - CRITICAL RULES

### 1. شريط BETA (BETA Banner)
- **موقعه:** أعلى جميع صفحات الموقع (fixed top-0)
- **الارتفاع:** 2rem (32px تقريباً)
- **المحتوى:** "BETA - New features weekly" + رابط "Share feedback"
- **اللون:** تدرج أخضر/أزرق/بنفسجي مع خلفية شفافة
- **z-index:** 50

**🚫 ممنوع منعاً باتاً:**
- إزالة شريط BETA
- إخفاء شريط BETA
- تغيير محتوى الشريط إلى "Development Mode"
- نسيان شريط BETA عند إضافة صفحات جديدة

---

### 2. التخطيطات (Layouts) والمكونات

**جميع المكونات التي تحتوي على headers أو navigation يجب أن:**

#### أ. الصفحات الرئيسية (Dashboard Pages)
```typescript
// في app/[locale]/(dashboard)/layout.tsx
<div className="lg:pl-[280px] pt-8"> {/* pt-8 للـ BETA banner */}
  <Header ... />
  <main className="min-h-[calc(100vh-4rem)] px-4 py-6 ...">
    {children}
  </main>
</div>
```

#### ب. الشريط الجانبي (Sidebar)
```typescript
// في components/layout/sidebar.tsx
<motion.aside
  className="fixed left-0 top-8 z-40 h-[calc(100vh-2rem)]" // top-8 و h-[calc(100vh-2rem)]
>
  {/* محتوى الشريط الجانبي */}
</motion.aside>
```

#### ج. الهيدر العام (Public Header)
```typescript
// في components/layout/public-header.tsx
<motion.header
  className="fixed top-8 z-40 w-full" // top-8 بدلاً من top-0
>
  {/* محتوى الهيدر */}
</motion.header>
```

#### د. صفحة الهبوط (Landing Page)
```typescript
// في app/[locale]/landing.tsx
<div className="min-h-screen bg-black text-white pt-8"> {/* pt-8 */}
  <header className="fixed top-8 left-0 right-0 z-40"> {/* top-8 */}
    {/* محتوى الهيدر */}
  </header>
  {/* باقي المحتوى */}
</div>
```

---

### 3. القيم المعيارية (Standard Values)

عند إضافة أي مكون جديد:

| الخاصية | القيمة | السبب |
|---------|--------|-------|
| `top` (للـ fixed/sticky) | `top-8` | لترك مساحة لشريط BETA |
| `padding-top` (للـ containers) | `pt-8` | لترك مساحة لشريط BETA |
| `height` (للـ sidebars) | `h-[calc(100vh-2rem)]` | لطرح ارتفاع شريط BETA |
| `z-index` (للـ BETA banner) | `z-50` | فوق كل شيء |
| `z-index` (للـ headers) | `z-40` | تحت شريط BETA |

---

## 🎨 مكونات BETA

### 1. BetaIndicator (الشريط العلوي)
**الموقع:** `components/common/beta-badge.tsx`

**الاستخدام:**
```typescript
import { BetaIndicator } from '@/components/common/beta-badge';

// في app/[locale]/layout.tsx
<BetaIndicator />
```

**الخصائص:**
- يظهر في جميع الصفحات
- لا يمكن إزالته أو إخفاؤه
- يحتوي على رابط للملاحظات (feedback@nnh.ae)

### 2. BetaBadge (الشارة)
**الموقع:** `components/common/beta-badge.tsx`

**الاستخدام:**
```typescript
import { BetaBadge } from '@/components/common/beta-badge';

<BetaBadge size="sm" variant="positive" />
```

**الخصائص:**
- `size`: 'xs' | 'sm' | 'md'
- `variant`: 'positive' | 'warning'

---

## 📝 Checklist عند إضافة صفحة/مكون جديد

### للـ UI/Layout:
- [ ] هل الصفحة/المكون يحتوي على header أو navigation؟
- [ ] هل تم إضافة `top-8` أو `pt-8` حسب الحاجة؟
- [ ] هل تم اختبار الصفحة للتأكد من عدم تغطية شريط BETA للمحتوى؟
- [ ] هل تم اختبار الصفحة في Dashboard و Public pages؟
- [ ] هل شريط BETA يظهر بشكل صحيح؟

### لـ ميزات GMB:
- [ ] **⚠️ هل راجعت الوثائق الرسمية في `google-api-docs/`؟**
- [ ] هل تحققت من الحقول المتاحة في API schema؟
- [ ] هل استخدمت الأنواع الصحيحة للبيانات؟
- [ ] هل تحققت من القيود (required, enum values, etc.)؟

---

## 🔍 استكشاف الأخطاء (Troubleshooting)

### المشكلة: شريط BETA يغطي المحتوى
**الحل:**
1. أضف `pt-8` للـ container الرئيسي
2. غيّر `top-0` إلى `top-8` للـ headers
3. استخدم `h-[calc(100vh-2rem)]` للـ sidebars

### المشكلة: شريط BETA لا يظهر في صفحة معينة
**الحل:**
1. تأكد أن `<BetaIndicator />` موجود في `app/[locale]/layout.tsx`
2. تأكد أن الصفحة تستخدم locale layout
3. تحقق من z-index للمكونات الأخرى

### المشكلة: المحتوى يبدأ تحت شريط BETA مباشرة بدون فراغ
**الحل:**
1. أضف `pt-8` للـ wrapper div الرئيسي
2. أو أضف `mt-8` للـ first child

---

## 📚 الملفات المرجعية

### الملفات المعدلة للـ BETA:
1. `package.json` - النسخة والوصف
2. `app/[locale]/layout.tsx` - إضافة BetaIndicator
3. `app/[locale]/(dashboard)/layout.tsx` - تعديل padding
4. `components/layout/sidebar.tsx` - تعديل positioning
5. `components/layout/public-header.tsx` - تعديل top position
6. `app/[locale]/landing.tsx` - تعديل padding و positioning
7. `components/common/beta-badge.tsx` - مكونات BETA
8. `app/[locale]/(dashboard)/changelog/page.tsx` - صفحة What's New
9. `messages/en.json` و `messages/ar.json` - إضافة "What's New"

### ملفات التوثيق:
1. `PROJECT_STATUS.md` - حالة المشروع
2. `BETA_STRATEGY.md` - استراتيجية BETA
3. `BETA_IMPLEMENTATION_SUMMARY.md` - ملخص التنفيذ
4. `DEPLOYMENT_GUIDE.md` - دليل النشر
5. `BETA_STATUS_GUIDE.md` - هذا الملف

---

## 🎯 أهداف BETA

1. **جمع الملاحظات:** الحصول على feedback من المستخدمين الأوائل
2. **التحديثات الأسبوعية:** إضافة ميزات جديدة كل أسبوع
3. **التطوير النشط:** تحسين مستمر بناءً على احتياجات المستخدمين
4. **الشفافية:** إعلام المستخدمين بحالة المشروع

---

## 🚀 الميزات القادمة

راجع صفحة **What's New** (`/changelog`) لمعرفة:
- آخر التحديثات
- الميزات الجديدة
- التحسينات
- إصلاحات الأخطاء

---

## 📧 التواصل

**للملاحظات والاقتراحات:**
- البريد الإلكتروني: feedback@nnh.ae
- الرابط في شريط BETA: "Share feedback"

---

## ⚡ ملاحظات نهائية

> **تذكّر دائماً:** هذا المشروع في حالة BETA ويجب أن تظهر مؤشرات BETA بوضوح في جميع الأوقات. عند إجراء أي تعديل على التخطيطات أو الـ navigation، تأكد من احترام مساحة شريط BETA.

> **القاعدة الذهبية:** إذا كنت تضيف أي مكون يحتوي على `fixed` أو `sticky` positioning، استخدم `top-8` بدلاً من `top-0`.

---

**آخر تحديث:** نوفمبر 18، 2025  
**النسخة:** 0.9.0-beta

