# 🎯 مرجع سريع - حالة BETA

## ⚠️ تذكير مهم
**المشروع في حالة BETA - لا تنسى ذلك عند أي تعديل!**

### 🌍 البيئات
```
🖥️ Development (localhost:5050)  → للتطوير الشخصي فقط
🌐 Production (nnh.ae)            → منشور للعالم كله ✅
```

> **المشروع على nnh.ae مُتاح للجميع حول العالم!**

---

## 📌 القواعد الأساسية

### 1. شريط BETA موجود دائماً
```
🟢 الموقع: أعلى جميع الصفحات
🟢 الارتفاع: 2rem (32px)
🟢 المحتوى: "BETA - New features weekly"
🚫 ممنوع: إزالته أو إخفاءه
```

### 2. القيم المعيارية للتخطيطات

| العنصر | الخاصية | القيمة الصحيحة |
|--------|---------|----------------|
| Header (fixed/sticky) | `top` | `top-8` ❌ ~~top-0~~ |
| Container الرئيسي | `padding-top` | `pt-8` |
| Sidebar | `top` | `top-8` |
| Sidebar | `height` | `h-[calc(100vh-2rem)]` |

---

## ✅ Checklist السريع

عند إضافة أي صفحة أو مكون جديد:

```
□ هل يحتوي على header أو navigation؟
  └─ نعم → استخدم top-8 بدلاً من top-0

□ هل يحتوي على container رئيسي؟
  └─ نعم → أضف pt-8

□ هل هو sidebar أو panel جانبي؟
  └─ نعم → استخدم top-8 و h-[calc(100vh-2rem)]

□ هل تم اختبار الصفحة؟
  └─ نعم → تأكد أن شريط BETA لا يغطي المحتوى
```

---

## 🔧 إصلاحات سريعة

### المشكلة: شريط BETA يغطي المحتوى
```typescript
// ❌ خطأ
<div className="pt-0">
  <header className="fixed top-0">

// ✅ صحيح
<div className="pt-8">
  <header className="fixed top-8">
```

### المشكلة: Sidebar مغطى بشريط BETA
```typescript
// ❌ خطأ
<aside className="fixed top-0 h-screen">

// ✅ صحيح
<aside className="fixed top-8 h-[calc(100vh-2rem)]">
```

---

## 📁 الملفات الرئيسية

```
components/common/beta-badge.tsx        → مكونات BETA
app/[locale]/layout.tsx                 → BetaIndicator (عام)
app/[locale]/(dashboard)/layout.tsx     → تخطيط Dashboard
components/layout/sidebar.tsx           → الشريط الجانبي
components/layout/public-header.tsx     → هيدر الصفحات العامة
app/[locale]/landing.tsx                → صفحة الهبوط
```

---

## 🎨 استخدام مكونات BETA

```typescript
// الشريط العلوي (يستخدم مرة واحدة في app/[locale]/layout.tsx)
import { BetaIndicator } from '@/components/common/beta-badge';
<BetaIndicator />

// الشارة (يمكن استخدامها في أي مكان)
import { BetaBadge } from '@/components/common/beta-badge';
<BetaBadge size="sm" variant="positive" />
```

---

## 💡 نصيحة ذهبية

> **قبل كل commit، اسأل نفسك:**  
> "هل أضفت أو عدلت أي layout/header/navigation؟"  
> **نعم** → تأكد من استخدام `top-8` و `pt-8`  
> **لا** → أكمل بأمان ✅

---

## 📚 للمزيد من التفاصيل

راجع: `BETA_STATUS_GUIDE.md` للدليل الكامل

---

**النسخة:** 0.9.0-beta  
**آخر تحديث:** نوفمبر 18، 2025

