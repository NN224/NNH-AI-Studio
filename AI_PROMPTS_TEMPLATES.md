# 📋 قوالب طلبات جاهزة للـ AI Agent

> **انسخ الطلب المناسب والصقه للـ AI Agent**

---

## 🔧 إصلاح مشاكل

### إصلاح مشكلة محددة:

```
أصلح المشكلة في ملف [اسم الملف]
المشكلة: [وصف المشكلة]
تأكد من:
- قراءة AI_AGENT_START_HERE.md أولاً
- البحث في production-fix-prompts/ عن prompt جاهز
- تشغيل npm run lint بعد الإصلاح
```

### إصلاح lint errors:

```
أصلح جميع lint errors في المشروع
شغّل npm run lint وأصلح كل الأخطاء
لا تتجاهل أي خطأ
```

### إصلاح build errors:

```
المشروع لا يعمل build
شغّل npm run build وأصلح كل الأخطاء
أرني الأخطاء وكيف أصلحتها
```

---

## ➕ إضافة ميزات

### إضافة ميزة جديدة:

```
أضف ميزة [وصف الميزة]
قبل البدء:
- اقرأ CLAUDE.md لفهم المشروع
- تأكد أن الميزة غير موجودة مسبقاً
- لا تنشئ ملفات جديدة إلا للضرورة
- استخدم الكود الموجود قدر الإمكان
بعد الانتهاء:
- شغّل npm run lint
- شغّل npm run build
```

### إضافة صفحة جديدة:

```
أضف صفحة [اسم الصفحة] في [المسار]
اتبع نفس نمط الصفحات الموجودة في app/[locale]/
استخدم نفس الـ layout والـ components الموجودة
```

### إضافة API endpoint:

```
أضف API endpoint جديد: [الوصف]
المسار: /api/[المسار]
اتبع نفس نمط الـ API routes الموجودة
تأكد من:
- التحقق من المستخدم (auth)
- معالجة الأخطاء
- إرجاع response مناسب
```

---

## 🔍 فحص وتحليل

### فحص الكود:

```
افحص ملف [اسم الملف]
أخبرني:
- ماذا يفعل هذا الملف؟
- هل هناك مشاكل؟
- كيف يمكن تحسينه؟
```

### فحص المشروع كامل:

```
افحص المشروع وأخبرني:
- هل هناك مشاكل أمنية؟
- هل هناك أخطاء في الكود؟
- ما الذي يحتاج تحسين؟
لا تعدل شيء، فقط أخبرني
```

### فحص قبل النشر:

```
افحص المشروع قبل النشر:
1. شغّل npm run lint
2. شغّل npm run build
3. شغّل npm run test
أخبرني بالنتائج وأي مشاكل
```

---

## 🗄️ قاعدة البيانات

### إضافة جدول جديد:

```
أضف جدول [اسم الجدول] في قاعدة البيانات
الحقول: [قائمة الحقول]
اتبع نمط الجداول الموجودة
أنشئ migration file في supabase/migrations/
حدّث DATABASE_TYPESCRIPT_INTERFACES.md
```

### تعديل جدول موجود:

```
عدّل جدول [اسم الجدول]
التعديل: [وصف التعديل]
أنشئ migration file جديد
لا تعدل migrations القديمة
```

---

## 🎨 واجهة المستخدم

### تعديل تصميم:

```
عدّل تصميم [اسم الصفحة/المكون]
التعديل المطلوب: [الوصف]
استخدم Tailwind CSS
لا تعدل ملفات components/ui/ (shadcn)
```

### إصلاح مشكلة عرض:

```
هناك مشكلة في عرض [الوصف]
الصفحة: [المسار]
المتوقع: [ما يجب أن يظهر]
الفعلي: [ما يظهر حالياً]
```

---

## 🚀 النشر

### قبل النشر:

```
جهّز المشروع للنشر:
1. تأكد من npm run lint يمر
2. تأكد من npm run build ينجح
3. تأكد من npm run test يمر
4. أخبرني إذا كل شيء جاهز
```

### بعد النشر:

```
تحقق من الموقع https://nnh.ae
هل كل شيء يعمل؟
أخبرني بأي مشاكل
```

---

## ⚠️ عند حدوث مشكلة

### الموقع لا يعمل:

```
الموقع لا يعمل
الخطأ: [انسخ رسالة الخطأ هنا]
ساعدني في حل المشكلة
```

### خطأ غير مفهوم:

```
ظهر هذا الخطأ:
[انسخ الخطأ هنا]
اشرح لي ما المشكلة وكيف أحلها
```

---

## 💡 نصائح للتواصل مع AI Agent

1. **كن محدداً**: بدل "أصلح المشكلة" قل "أصلح خطأ التحميل في صفحة المراجعات"
2. **أعطِ سياق**: اذكر اسم الملف أو الصفحة
3. **انسخ الأخطاء**: إذا ظهر خطأ، انسخه كاملاً
4. **اطلب التأكيد**: قل "تأكد من npm run lint" في نهاية الطلب
5. **راجع النتيجة**: اسأل "هل انتهيت؟ أرني ملخص ما فعلت"

---

## 🔄 طلب متابعة

### بعد أي تعديل:

```
أرني ملخص ما فعلت:
- ما الملفات التي عدلتها؟
- ما التغييرات التي أجريتها؟
- هل شغّلت npm run lint؟
- هل كل شيء يعمل؟
```

### إذا لم يكتمل العمل:

```
أكمل العمل السابق
لا تبدأ من جديد
```
